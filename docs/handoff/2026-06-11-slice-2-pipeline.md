# Handoff: slice 1 shipped and live — next: slice 2 (issues #12–#15)

**Date:** 2026-06-11
**Repo:** `D:\planned-program-intel\planned-program-intel` → https://github.com/pixiiidust/planned-program-intel (branch `main`, clean)
**Live demo (THE application artifact — protect it):** https://pixiiidust.github.io/planned-program-intel/ — since commit `ca3700a` this serves the **real build**, not the prototype. `/app/` redirects to root.
**Previous handoff (superseded):** `docs/handoff/2026-06-11-prd-to-issues.md`

## Mission for the next session

Implement **slice 2 — pipeline + real corpus**: issues **#12 → #13 → #14** (linear chain) and **#15** (independent, unblocked). Read each issue body first — they are the spec. Jamie approved working straight on `main` with commits closing issues (`Closes #N`); each issue ends with CI green and the live demo upgraded.

- **#12** Pipeline skeleton: `packages/pipeline` workspace (doesn't exist yet), embed & emit stage over the existing seed, local open-source embedding model (no API key), emits decision→cases + decision→decisions tables replacing the hand-authored stopgap, CI validates the seed bundle against domain contracts (build-failing).
- **#13** Corpus generation (~300 Cases) + outcome labelling. **Hard rule (ADR-0004): outcomes/counts never from the LLM.** Emits diffable proposal files; **must be exercised via an actual review PR** — the one place the straight-to-main workflow deliberately doesn't apply, because review-as-PR is the feature being demonstrated.
- **#14** Pattern clustering & naming + Exception detection → seed v2 live. LLM (via AI port: Anthropic Haiku primary, Ollama alternative, **canned in CI**) only names and narrates. Consult `/claude-api` before writing the LLM stages.
- **#15** Simulated feed: Signal-typed ingestion port, one scripted `registration.pace_updated` Signal ~20s after first visit (delay must be configurable for e2e), labeled "simulated feed", reset re-arms it; design note at `docs/design/decision-detection.md`.

## What happened this session (read artifacts, don't re-derive)

1. Tracker was wiped and rebuilt: old issues #1–#6 deleted. The PRD now lives at **`docs/prd/2026-06-11-real-build-six-slices.md`** (source of record; no PRD issue). All 18 tracer-bullet issues published: **#7–#24** with real cross-references. HITL gates: #18 (portfolio variant round), #20 (Cloudflare/Anthropic provisioning), #24 (README narrative review).
2. **Slice 1 (#7–#11) fully implemented, all closed**, commits `94c0d3f`, `92c7bf8`, `00871e1`, `e5071c4`, `ca3700a` — read the commit messages; they are accurate. 20 unit tests + 16 e2e, all green; CI verified after every push; live cutover verified by HTTP.

## Repo facts a fresh agent needs (not obvious from the issues)

- **Monorepo:** npm workspaces — `packages/domain` (types/ports/pure logic; imports nothing), `packages/adapters`, `apps/web`. `packages/pipeline` and `apps/edge` do **not** exist yet (#12 / #20 create them). Internal packages expose TS source directly (`"main": "src/index.ts"`); Vite/Vitest compile it. Node 24, npm 11.
- **Seed:** generated by `scripts/convert-prototype-seed.mjs` (mechanical conversion of `prototype/src/data.js` + hand-authored siblings d17–d19 and escalated d20) into `packages/adapters/src/demo/seed.ts` — `SEED satisfies SeedBundle`, version `seed-v1-hand-converted`. The sibling map is `SeedBundle.siblings`; `App.tsx` reads `DEMO_SEED.siblings` for Precedent landing. **#12/#14 replace this script and file; bump `seedVersion` — the IndexedDB adapter nukes-and-reseeds on mismatch (that's the migration story, by design).**
- **Ports (domain):** `DecisionRepository` (load/save/reset — IndexedDB adapter `packages/adapters/src/demo/indexedDbRepository.ts`); `DecisionSource` exists but the app now loads via the repository. #15's ingestion port is new work.
- **Tests:** Vitest unit tests live next to source in `packages/`; Playwright e2e in `e2e/` (`inbox`, `detail`, `resolve`, `journey` specs). Several e2e assertions hardcode seed facts (counts like `Decided (7)`/`(8)`, titles, `41 of 48`, `85%`) — **the slice-2 corpus will break them; update them deliberately, they're the deploy gate.** Config: `vitest.config.ts` (excludes `e2e/`), `playwright.config.ts` (webServer = build + preview on 4173).
- **CI/deploy:** one workflow, `.github/workflows/deploy-pages.yml`: verify (typecheck → unit → build → e2e) gates the Pages deploy; a red run keeps the previous demo live. Root = `apps/web/dist` (Vite `base: './'`), plus an `/app/` redirect stub written inline in the workflow.
- **Windows/PowerShell gotchas that cost time:** (1) never put double-quote characters inside `git commit -m @'...'@` here-strings — PS 5.1 native-arg quoting breaks the message into pathspecs; (2) a zero-width space once snuck into a written file's first byte — if YAML/JSON mysteriously fails to parse, check bytes; (3) `&&` doesn't exist in PS 5.1, use `;` or `if ($?)`.

## Working agreements (from Jamie, this session)

- Work autonomously through the slice; commit straight to `main` (except #13's review PR — see above). Watch each CI run (`gh run watch`) before calling an issue done.
- Pause at HITL issues; don't start #18/#20/#24 content without Jamie.
- The demo link is a live job application: never leave it broken; stale beats broken is already enforced by the gate, but also verify the deployed URL after cutover-class changes.
- Jamie approves recommendations quickly but reads carefully; present options with a clear rec when a real fork appears.

## Suggested skills

- **`/tdd`** — pipeline stages and the seed validator are pure-logic-heavy; domain tests are "the executable form of CONTEXT.md".
- **`/claude-api`** — REQUIRED before writing #14's LLM stages or any Anthropic adapter (model ids, canned/eval patterns; ADR-0002 records the choices).
- **`/handoff`** — at session end, write the slice-3 handoff into `docs/handoff/`.

## Authoritative companion docs

`CONTEXT.md` (glossary) · `docs/adr/0001–0005` · `docs/prd/2026-06-11-real-build-six-slices.md` (PRD + all 6 slices) · `prototype/NOTES.md` (UI verdicts) · issues #12–#24 on GitHub.
