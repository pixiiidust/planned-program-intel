# Handoff: slice 5.2 live (#21 closed, engine picker + eval split shipped) — next: #27 design conversation, then slice 6

**Date:** 2026-06-12 (seventh session of the day)
**Repo:** `D:\planned-program-intel\planned-program-intel` → https://github.com/pixiiidust/planned-program-intel
**Live demo (THE job-application artifact — protect it):** https://pixiiidust.github.io/planned-program-intel/ — serving slice 5.2 (commit `609ecdb`, CI run 27435596646 green, Pages deployed). **37/37 e2e, 136 vitest.**
**Branch:** everything on `main`, working tree clean.
**Previous handoff (superseded):** `2026-06-12-slice-5-1-live.md` — its working agreements, environment notes, and gotchas still apply except where this doc updates them.

## What landed this session (do not redo)

1. **#21 closed — engine picker + settings drawer + eval split** (`609ecdb`, read its message for the full inventory). `SettingsDrawer.tsx` (Demo / BYO-key / Local-Ollama radio picker writing the established storage contract, reset-demo-data moved in from the header), setter helpers in `apps/web/src/lib/distillation.ts`, `packages/pipeline/src/evalDistill.ts` + `eval:distill` script with `.env` loading extracted to `packages/pipeline/src/env.ts`, e2e: `settings.spec.ts` (3 specs) + reset specs rerouted through the drawer. Spec source was `docs/handoff/2026-06-12-codex-brief-slice5b.md` (committed, including the #27 extension).
2. **#27 settled half shipped in the same commit:** manual `×` on the toast (`aria-label="Dismiss notification"`), 4s auto-dismiss and all asserted toast strings untouched, covered by the `resolve.spec.ts` 'toast can be dismissed manually' e2e. Progress comment posted on #27.
3. **Live eval validated once against real Haiku via OpenRouter: 8/8 fixtures pass.** The prompt-injection fixture was cleanly neutralized (no digits, injection ignored, legitimate reasoning distilled). The script prints raw outputs for the human read and exits 1 on any mechanical failure. It spends the PIPELINE key — manual only, never CI.
4. **One Codex fix-up round total:** `getByLabel('Model')` / `getByLabel('Ollama endpoint')` were ambiguous because radio accessible names include the option description copy — fixed by scoping to `getByRole('textbox', ...)`. Remember this for future drawer e2e.

## Mission for the next session

1. **#27 unsettled half — the reviewable record of recent activity — is a DESIGN CONVERSATION WITH JAMIE, not a build.** Candidate directions and binding constraints are in the issue (ADR-0002 honest visibility; must not become a second Portfolio; resolution stays deterministic/non-blocking). Jamie's collapsible-sidebar sketch is a problem statement, not a spec. A variant round (`/prototype`) is a plausible vehicle once directions are agreed.
2. **Slice 6 (hardening + product narrative), issues already filed:** #22 Postgres adapter + one-suite-two-adapters contract tests, #23 daily live-link smoke cron, #24 README as product story. All `ready-for-agent`.
3. Never leave the live demo broken: deploys are gated on e2e; stale beats broken. AI stays progressive enhancement.

## Working setup (unchanged)

Read memory `codex-execution-workflow.md`: Fable plans/reviews/verifies/commits; Codex (gpt-5.5 xhigh) makes ALL production edits via the openai-codex plugin companion (plugin cache version still 1.0.4). Tell Codex to verify with `npm.cmd`; Fable runs Playwright. Fix-ups via `task --resume-last`.

## Gotchas (new this session, on top of the previous handoff's)

- **Radio option labels swallow description copy:** any `getByLabel(...)` inside the settings drawer must not collide with the radio descriptions ("free models… model id", "Your Ollama endpoint…"). Scope field locators to `getByRole('textbox', ...)`.
- Codex hit mojibake-related patch misses on `✦`/`—` characters in existing files and recovered by anchoring on ASCII context — expect an extra round when briefs target lines containing those glyphs.
- `eval:distill` lives only in `packages/pipeline/package.json`; root `validate`/`test`/`typecheck`/`build` and CI workflows are untouched (verified).
- Load-time counts unchanged (Needs you 12 / Waiting 1 / Decided 7; `?feedDelay=0` for count-asserting e2e).

## Environment notes (carried forward, see previous handoff for detail)

- Repo-root `.env` (gitignored) holds the PIPELINE OpenRouter key — never echo, never commit, never use for the Worker. The Worker's dedicated key lives only in Cloudflare's secret store; no Worker changes this session.
- e2e suite baseline: `e2e/fixtures.ts` aborts `**/distill`; new specs import `{ expect, test }` from `./fixtures`; success-path specs re-route their own engine URL (`**/distill`, `**/openrouter.ai/**`, `**/api/chat`).
- No dev servers left running.

## Suggested skills

- **`/prototype`** — if the #27 design conversation converges on wanting visual variants of the activity surface.
- **`/claude-api`** — REQUIRED before touching the distillation prompt, `evalDistill.ts`, or any LLM-stage code (repo rule).
- **`/handoff`** — at session end, into `docs/handoff/`, push.

## Authoritative companion docs

`CONTEXT.md` (Distillation glossary) · `docs/adr/0002` · the #21 conclusion comments (now closed) · issue #27 (open, constraints + candidate directions) · commit `609ecdb` · `docs/handoff/2026-06-12-codex-brief-slice5b.md` · `docs/handoff/2026-06-12-slice-5-1-live.md` · memory `codex-execution-workflow.md`.
