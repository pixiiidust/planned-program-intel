# Handoff: slice 5.1 live (#20 closed, Worker deployed) — next: #21 (engine picker + eval split)

**Date:** 2026-06-12 (sixth session of the day)
**Repo:** `D:\planned-program-intel\planned-program-intel` → https://github.com/pixiiidust/planned-program-intel
**Live demo (THE job-application artifact — protect it):** https://pixiiidust.github.io/planned-program-intel/ — serving the slice-5.1 build (commit `5283a66`, CI run 27433491372 green). **33/33 e2e, 136 vitest.**
**Edge Worker (LIVE):** `https://ppi-distill.pixiiidust.workers.dev/distill` — verified end-to-end: disallowed origin 403, preflight 204, malformed input 400, real Haiku distillation round-trip in 1.5s (one sentence, no digits).
**Branch:** everything on `main`, working tree clean.
**Previous handoff (superseded):** `2026-06-12-slice-4-complete.md` — its working agreements still apply except where this doc updates them.

## What landed this session (do not redo)

1. **#20 closed — the live AI moment** (`5f360c2`, read its message): shared distillation module in `packages/adapters/src/ai/distillation.ts` (prompt builder, digit/sentence/empty guards, ~1,000-char clamp, real 3s fetch-abort via injected `fetchImpl`), 8 guard fixtures + CI tests, `createDemoProxyAi`, `apps/edge` Worker with the full ADR-0002 cap stack, `Precedent.distilledBy` + pure `distillPrecedentText`, toast `✦`→`✦ distilled`→wordless fade, permanent provenance chip with engine tooltip, live crossfade, re-persisted swaps. One Codex fix-up round total (deep imports, see gotchas).
2. **Worker deployed via HITL with Jamie** (`5283a66`): free Cloudflare account, subdomain `pixiiidust.workers.dev`, KV namespace `902c11ac8a8b468692fae71dafe07500` (in wrangler.toml), ratelimit binding live. **Dedicated** OpenRouter key `ppi-edge-worker` ($10 cap) exists ONLY in Cloudflare's secret store — the pipeline key in `.env` is untouched and unrelated; either can be revoked alone. Deploys are manual `npx wrangler deploy` from `apps/edge` (never CI).
3. **e2e architecture decision:** `e2e/fixtures.ts` aborts `**/distill` for the whole suite — the degradation contract (verbatim stays) is the deterministic baseline and CI can never hit the live Worker or spend. New specs MUST import `{ expect, test }` from `./fixtures`. A spec that wants the success path re-routes `**/distill` itself (newer routes win).
4. **Engine settings storage contract is already implemented** in `apps/web/src/lib/distillation.ts` (read side): localStorage `ppi-engine` / `ppi-byok-model` / `ppi-ollama-endpoint` / `ppi-ollama-model`; sessionStorage `ppi-byok-key` (NEVER localStorage). The #21 drawer only needs setters + UI.

## Mission for the next session

1. **#21 — engine picker + settings drawer + eval split.** The Codex brief is ALREADY WRITTEN and reviewed: `docs/handoff/2026-06-12-codex-brief-slice5b.md` (committed alongside this handoff). Launch it as-is via the codex companion; spec source of truth remains the #21 conclusion comment. Note the brief moves "Reset demo data" into the drawer — `e2e/resolve.spec.ts` and `e2e/feed.spec.ts` reset interactions must be updated (the brief covers this).
2. After #21: slice 6 (hardening + product narrative): Postgres adapter + contract suite, daily smoke cron, README as product story.
3. Never leave the live demo broken: deploys are gated on e2e; stale beats broken.

## Working setup (unchanged — read memory `codex-execution-workflow.md`)

Fable plans/reviews/verifies/commits; Codex (gpt-5.5 xhigh) makes ALL production edits via the openai-codex plugin companion (brief in a temp file, Bash `run_in_background`, fix-ups via `task --resume-last`). Tell Codex to verify with `npm.cmd`; reviewer (Fable) runs the e2e.

## Gotchas (new this session)

- **The auto-mode classifier denies Fable's `npm install`** when the conversation contains "Do NOT run npm install" (the constraint written for the offline Codex executor). Don't fight it — ask Jamie to type `! npm install`. Also: a re-run that prints "up to date" does NOT execute newly-allowlisted postinstalls; if `wrangler deploy` complains about missing esbuild/workerd binaries, run `npm rebuild esbuild workerd`.
- **`apps/edge` must deep-import adapters' ai modules** (`@ppi/adapters/src/ai/distillation`, extensionless) — the barrel pulls in the IndexedDB demo adapter whose DOM types don't exist under the Workers lib.
- **PowerShell 5.1 mangles embedded JSON quotes passed to `curl.exe`** — a valid-looking request 400s. Write the body to a file and use `--data-binary "@file"`.
- **wrangler's "unsafe fields are experimental" warning is benign** — it's the ratelimit binding, working as configured.
- Root `package.json` `allowScripts` gained `esbuild@0.27.3` + `workerd@1.20260611.1` (wrangler deps).
- Load-time counts unchanged (Needs you 12 / Waiting 1 / Decided 7; `?feedDelay=0` for count-asserting e2e). Composite e2e-asserted strings stay single text nodes (`✦ distilled` is one node in toast and chip).

## Environment notes (carried forward)

- Repo-root `.env` (gitignored) holds the PIPELINE OpenRouter key — never echo, never commit, never use for the Worker.
- Worker spend is observable at openrouter.ai → Activity (key `ppi-edge-worker`); Cloudflare dashboard → Workers → ppi-distill for request counts. Caps: 160 max_tokens, 6 req/min/IP, ~300/day KV ceiling.
- Codex must call `npm.cmd` (PowerShell execution policy); the Bash tool is unaffected.
- Pipeline regen order: `embed → cluster → name → emit → validate`; narration regens merge around reviewed entries.
- No dev servers left running.

## Suggested skills

- **`/claude-api`** — REQUIRED before touching the distillation prompt, the live-eval script, or any LLM-stage code (repo rule; #21's eval script qualifies).
- **`/handoff`** — at session end, into `docs/handoff/`, push.
- **`/grill-with-docs`** — only if implementation uncovers a genuine spec contradiction; #21 is fully settled.

## Authoritative companion docs

`CONTEXT.md` (Distillation glossary entry) · `docs/adr/0002` (amended; the degradation contract's full text) · `docs/adr/0005` · the #20/#21 conclusion comments · commits `5f360c2`, `5283a66` · `docs/handoff/2026-06-12-codex-brief-slice5b.md` (the ready #21 brief) · memory `codex-execution-workflow.md`.
